package gui;
/**
 * An interface that every class implements, which is capable creating sound in some way.
 */
public interface PlayfieldSound {
	/**
	 * Plays a sound.
	 */
	public void playSound(String fileName);

}
